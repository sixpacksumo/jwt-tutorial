<%= form_with(model: admin, local: true) do |form| %>
  <% if admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
      <% admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_area :username, id: :admin_username %>
  </div>

  <div class="field">
    <%= form.label :password_digest %>
    <%= form.text_area :password_digest, id: :admin_password_digest %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
